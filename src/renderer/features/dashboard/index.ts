/**
 * Dashboard feature barrel (task 13.1 placeholder; task 13.2 swap-in).
 *
 * Until task 13.2 lands its full implementation, the placeholder
 * dashboard ships from this barrel so the role-aware shell can route
 * Admins to a stable `/dashboard` home.
 */

export { DashboardPage } from './DashboardPage';
