# Local development DATABASE_URL for Prisma CLI (migrate dev / generate / db seed).
# At runtime in the packaged Electron app, the main process points Prisma at
# <userData>/shop.db instead — see src/main/db/prisma.ts (task 1.3).
DATABASE_URL="file:./dev.db"
